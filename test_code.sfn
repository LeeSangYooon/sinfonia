class Cell {
    bool mine;
    bool revealed;
}

class Position {
    int x;
    int y;

    # int, int -> none
    init x y {
        this.x = x;
        this.y = y;
        int c = 12 * 320 + this.y * 13 - 2 * 4 + 1;
    }
}