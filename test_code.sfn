class Cell {
    bool mine;
    bool revealed;
}

class Position {
    int x;
    int y;

    # int, int -> none
    init x y {
        print (1 > 2 and b < (c == 1) + 4 or c == b and 1 > 3);
        this.x = x;
        this.y = y;
        let int c = 12 * 320 + this.y * 13 - 2 * 4 + 1;
        this.x = c * 2 - this.y;
        if c {
            print (c 2) * 2 - 1;
            a = c * 2 -1;
            print (a - (b == c) + d > 1);
            for i in range 10 {
                print i;
                
            }
            while a 12 - this.x {
                print x 1;
            }
        }
        while a 12 - this.x {
                print x 1;
            }
    }
}