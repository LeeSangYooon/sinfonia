// factorial을 구하는 함수
# int -> int
factorial n {
    // n이 0이라면 1을 반환합니다.
    if n == 0 { 
        return 1; 
    }
    // n! = n * (n-1)!을 이용해 재귀적으로 값을 구합니다.
    return n * factorial(n - 1);
}

print(factorial(6)); // 6! = 720


